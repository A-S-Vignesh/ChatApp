# Force `npm install` to honor devDependencies even when NODE_ENV=production
# (e.g. on Render). Belt-and-suspenders alongside having build-essential types
# in `dependencies` — keeps the build working regardless of the deploy host's
# default install mode.
production=false
